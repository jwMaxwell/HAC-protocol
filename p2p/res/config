# Example config file. Lines beginning with # are considered comments and are
# skipped. Lines containing node records follow the format
#    id, address, port
# where id is the node's system-wide id number, address is the node's IP
# address or hostname, and port is the port number on which the node's HAC P2P
# application is listening. The default port is 9876 (there is currently no way
# to use another port when launching via the command line)

1, 192.168.0.109, 9876